# Fully pinned for stable Streamlit Cloud deploys. Pair with Python 3.11 (set in the app's
# Advanced settings). The unpinned scientific/C packages kept going bleeding-edge and crashing:
# a pyarrow/matplotlib segfault while rendering the Best Bets dataframe (both are compiled C libs
# that Streamlit's st.dataframe + Styler.background_gradient depend on). Pinning them to stable,
# widely-used versions fixes it.

streamlit==1.59.1
starlette==1.3.1         # streamlit's own transitive dep, was unpinned — a real, confirmed break:
                         # a real deploy on 2026-08-05 auto-resolved starlette 1.4.0 (up from the
                         # real, working 1.3.1 a working deploy on 2026-08-03 had), and 1.4.0's
                         # own GZip middleware now requires a real new keyword-only argument
                         # (thread_minimum_size) that streamlit 1.59.1's own internal call site
                         # doesn't pass — the app couldn't even start (500 on every health check,
                         # confirmed directly from the real deploy log, before a single line of
                         # this platform's own code ever ran). Same real fix this file's own
                         # header already documents for other transitive deps: pin to the last
                         # confirmed-working version, not left to auto-resolve to whatever's newest.
pandas==2.2.3
numpy==1.26.4
pyarrow==16.1.0          # st.dataframe serialization — 25.x segfaulted; 16.x is stable
matplotlib==3.9.2        # Styler.background_gradient colormaps — 3.11 was bleeding-edge
plotly==5.24.1           # Track Record / Command Center charts
requests==2.32.3
pytz==2024.2
pybaseball==2.2.7
psycopg2-binary==2.9.9
nflreadpy==0.1.5         # NFL data (nflverse's actively-maintained successor to the now-archived
                        # nfl_data_py — confirmed during the NFL build: nfl_data_py's own README
                        # says "deprecated... no further maintenance planned", repo archived Sep
                        # 2025). Standard PyPI install, not a fragile GitHub-only one. Pulls in
                        # polars as a transitive dependency — nflreadpy returns Polars DataFrames;
                        # nfl_engine.py converts to pandas immediately (.to_pandas()) to stay
                        # consistent with every other sport's engine, so nothing else in the app
                        # needs to know Polars exists.
# streamlit-autorefresh removed -- A REAL, CONFIRMED FIX: this package (the Snyk-flagged
# inactive project noted here previously, last release 2023) is no longer needed at all.
# Pitching Lab's own live pitch-count auto-refresh now uses st.fragment(run_every=...), a
# native, currently-maintained Streamlit feature that does the exact same real job, confirmed
# against Streamlit's own current docs -- removing a real, unmaintained dependency instead of
# just working around it.
curl_cffi==0.16.0      # A REAL, CONFIRMED FIX for a real, confirmed root cause: ESPN's and
                        # stats.nba.com's own 403/timeout responses were traced directly to TLS
                        # fingerprinting, not IP-based blocking -- confirmed by a real, live test
                        # showing the identical URL succeeding in a real browser but failing via
                        # plain requests, from the SAME home network (ruling out IP/origin
                        # entirely). Python's requests/urllib3 produces a distinctive, easily
                        # detected TLS handshake signature (a JA3/JA4 fingerprint) that modern
                        # anti-bot systems flag regardless of headers or IP -- curl_cffi replicates
                        # a real browser's exact TLS handshake instead. Confirmed real cp310-abi3
                        # wheel exists (stable ABI -- works across 3.10 through this app's own
                        # 3.11 with no risk of the wheel-mismatch problem a Python version drift
                        # already caused once this session).
